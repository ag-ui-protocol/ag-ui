import { MenuIntegrationConfig } from "./types/integration";
import { integrations } from "./integrations";

/**
 * Menu integrations - derived from the single source of truth in integrations.ts
 */
export const menuIntegrations: MenuIntegrationConfig[] = integrations;
