//! Pulling A2UI out of a model's response.
//!
//! When A2UI is generated by prompting rather than by structured output, the
//! model returns prose with one or more A2UI blocks embedded in it, fenced by
//! [`A2UI_OPEN_TAG`] and [`A2UI_CLOSE_TAG`]. [`parse_response`] splits that into
//! ordered [`ResponsePart`]s: the conversational text preceding each block, and
//! the block's parsed JSON.
//!
//! # Why this is a scanner and not a string split
//!
//! A close tag can appear inside a JSON string literal — a `Text` component
//! whose content mentions `</a2ui-json>` is perfectly legal — so the scanner
//! tracks string state and escapes rather than searching for the next
//! occurrence.
//!
//! # Repairing model output
//!
//! [`parse_and_fix`] applies the two repairs that are safe because they cannot
//! change the meaning of valid JSON: normalizing smart quotes, and dropping
//! trailing commas. It only reaches for them after a straight parse has failed,
//! and it wraps a lone object in an array, since A2UI payloads are always lists
//! of messages.

use serde_json::Value;

use crate::constants::{A2UI_CLOSE_TAG, A2UI_OPEN_TAG};
use crate::error::{Error, Result};

/// One segment of a model response.
#[derive(Debug, Clone, PartialEq, Default)]
pub struct ResponsePart {
    /// Conversational text that preceded this part's A2UI block, trimmed and
    /// stripped of Markdown fences. Empty when the block came first.
    pub text: String,
    /// The raw block content, before parsing.
    pub raw: Option<String>,
    /// The parsed A2UI messages, when the block parsed.
    pub a2ui: Option<Vec<Value>>,
    /// Whether the block was closed. A truncated stream ends with `false`.
    pub is_final: bool,
}

/// Whether the content contains a complete A2UI block.
///
/// Both tags must be present; a response that opens a block and stops mid-stream
/// does not count.
pub fn has_a2ui_parts(content: &str) -> bool {
    content.contains(A2UI_OPEN_TAG) && content.contains(A2UI_CLOSE_TAG)
}

/// Splits a full model response into text and parsed A2UI blocks.
///
/// # Errors
///
/// Returns [`Error::Parse`] when the response contains no A2UI block, when a
/// block is empty, when a block is unterminated, or when a block's content is
/// not JSON even after repair.
pub fn parse_response(content: &str) -> Result<Vec<ResponsePart>> {
    let mut parts = unwrap_response(content)?;
    for part in &mut parts {
        if let Some(raw) = &part.raw {
            part.a2ui = Some(parse_and_fix(raw)?);
        }
    }
    Ok(parts)
}

/// Splits a response into parts without parsing the block contents.
///
/// # Errors
///
/// See [`parse_response`], minus the JSON failures.
pub fn unwrap_response(content: &str) -> Result<Vec<ResponsePart>> {
    let parts = tokenize(content);
    let mut out = Vec::new();
    let mut saw_block = false;

    for part in parts {
        match &part.raw {
            Some(raw) => {
                if !part.is_final {
                    return Err(Error::parse(format!(
                        "A2UI close tag '{A2UI_CLOSE_TAG}' not found in response."
                    )));
                }
                if raw.is_empty() {
                    return Err(Error::parse("A2UI JSON part is empty."));
                }
                saw_block = true;
                out.push(part);
            }
            None => {
                if !part.text.is_empty() {
                    out.push(part);
                }
            }
        }
    }

    if !saw_block {
        return Err(Error::parse(format!(
            "A2UI tags '{A2UI_OPEN_TAG}' and '{A2UI_CLOSE_TAG}' not found in response."
        )));
    }
    Ok(out)
}

/// Parses a raw A2UI block, repairing the model's usual JSON slips.
///
/// Always returns a list: a lone object is wrapped, since an A2UI payload is a
/// list of messages.
///
/// # Errors
///
/// Returns [`Error::Parse`] when the content is not JSON even after repair.
pub fn parse_and_fix(payload: &str) -> Result<Vec<Value>> {
    let normalized = normalize_smart_quotes(payload);
    match parse_list(&normalized) {
        Ok(value) => Ok(value),
        // Only now try the lossy repair, so valid JSON is never rewritten.
        Err(first) => parse_list(&remove_trailing_commas(&normalized)).map_err(|_| first),
    }
}

fn parse_list(payload: &str) -> Result<Vec<Value>> {
    let value: Value = serde_json::from_str(payload)
        .map_err(|e| Error::parse(format!("Failed to parse JSON: {e}")))?;
    Ok(match value {
        Value::Array(items) => items,
        other => vec![other],
    })
}

fn normalize_smart_quotes(input: &str) -> String {
    input
        .replace(['\u{201C}', '\u{201D}'], "\"")
        .replace(['\u{2018}', '\u{2019}'], "'")
}

/// Drops commas that directly precede a closing bracket or brace.
///
/// String-aware, so `{"text": "a, b"}` and `["x,"]` are untouched.
fn remove_trailing_commas(input: &str) -> String {
    let chars: Vec<char> = input.chars().collect();
    let mut out = String::with_capacity(input.len());
    let mut i = 0;
    let mut in_string = false;

    while i < chars.len() {
        let ch = chars[i];
        if in_string {
            out.push(ch);
            if ch == '\\' {
                if let Some(next) = chars.get(i + 1) {
                    out.push(*next);
                    i += 2;
                    continue;
                }
            } else if ch == '"' {
                in_string = false;
            }
            i += 1;
            continue;
        }
        match ch {
            '"' => {
                in_string = true;
                out.push(ch);
            }
            ',' => {
                let next = chars[i + 1..].iter().find(|c| !c.is_whitespace()).copied();
                if !matches!(next, Some(']') | Some('}')) {
                    out.push(ch);
                }
            }
            _ => out.push(ch),
        }
        i += 1;
    }
    out
}

/// Scans a response into alternating text and block parts.
fn tokenize(content: &str) -> Vec<ResponsePart> {
    let chars: Vec<char> = content.chars().collect();
    let mut parts = Vec::new();
    let mut text = String::new();
    let mut raw = String::new();
    let mut i = 0;
    let mut in_block = false;

    while i < chars.len() {
        if !in_block {
            if let Some(end) = match_open_tag(&chars, i) {
                in_block = true;
                raw.clear();
                i = end;
                continue;
            }
            text.push(chars[i]);
            i += 1;
            continue;
        }

        if let Some(end) = match_close_tag(&chars, i) {
            parts.push(ResponsePart {
                text: clean_markdown(&text),
                raw: Some(clean_markdown(&raw)),
                a2ui: None,
                is_final: true,
            });
            text.clear();
            raw.clear();
            in_block = false;
            i = end;
            continue;
        }

        // Inside a JSON string literal a close tag is just text.
        let ch = chars[i];
        if ch == '"' || ch == '\'' {
            raw.push(ch);
            i += 1;
            while i < chars.len() {
                if chars[i] == '\\' {
                    raw.push(chars[i]);
                    if let Some(next) = chars.get(i + 1) {
                        raw.push(*next);
                    }
                    i += 2;
                    continue;
                }
                raw.push(chars[i]);
                let closed = chars[i] == ch;
                i += 1;
                if closed {
                    break;
                }
            }
            continue;
        }
        raw.push(ch);
        i += 1;
    }

    if in_block {
        parts.push(ResponsePart {
            text: clean_markdown(&text),
            raw: Some(clean_markdown(&raw)),
            a2ui: None,
            is_final: false,
        });
    } else {
        let trailing = clean_markdown(&text);
        if !trailing.is_empty() {
            parts.push(ResponsePart {
                text: trailing,
                raw: None,
                a2ui: None,
                is_final: true,
            });
        }
    }
    parts
}

/// Matches `<a2ui-json ...>` at `start`, returning the index just past it.
///
/// Attributes are tolerated; the tag name must end on a word boundary so
/// `<a2ui-jsonx>` does not match.
fn match_open_tag(chars: &[char], start: usize) -> Option<usize> {
    let name: Vec<char> = A2UI_OPEN_TAG
        .trim_start_matches('<')
        .trim_end_matches('>')
        .chars()
        .collect();
    if chars.get(start) != Some(&'<') {
        return None;
    }
    let mut i = start + 1;
    for expected in &name {
        let actual = chars.get(i)?;
        if !actual.eq_ignore_ascii_case(expected) {
            return None;
        }
        i += 1;
    }
    if chars
        .get(i)
        .is_some_and(|c| c.is_alphanumeric() || *c == '_')
    {
        return None;
    }
    while let Some(ch) = chars.get(i) {
        if *ch == '>' {
            return Some(i + 1);
        }
        i += 1;
    }
    None
}

/// Matches `</a2ui-json  >` at `start`, returning the index just past it.
fn match_close_tag(chars: &[char], start: usize) -> Option<usize> {
    let name: Vec<char> = A2UI_CLOSE_TAG
        .trim_start_matches("</")
        .trim_end_matches('>')
        .chars()
        .collect();
    if chars.get(start) != Some(&'<') || chars.get(start + 1) != Some(&'/') {
        return None;
    }
    let mut i = start + 2;
    for expected in &name {
        let actual = chars.get(i)?;
        if !actual.eq_ignore_ascii_case(expected) {
            return None;
        }
        i += 1;
    }
    while chars.get(i).is_some_and(|c| c.is_whitespace()) {
        i += 1;
    }
    (chars.get(i) == Some(&'>')).then_some(i + 1)
}

/// Trims whitespace and strips a surrounding Markdown code fence.
fn clean_markdown(text: &str) -> String {
    let mut text = text.trim();
    if let Some(rest) = text.strip_prefix("```") {
        // Drop an optional language tag on the opening fence.
        let rest = rest.trim_start_matches(|c: char| c.is_alphanumeric() || c == '-');
        text = rest.trim_start();
    }
    if let Some(rest) = text.strip_suffix("```") {
        text = rest.trim_end();
    } else if let Some(index) = text.rfind("```") {
        let tail = &text[index + 3..];
        if !tail.is_empty() && tail.chars().all(|c| c.is_alphanumeric() || c == '-') {
            text = text[..index].trim_end();
        }
    }
    text.trim().to_string()
}

#[cfg(test)]
mod tests {
    use super::*;
    use serde_json::json;

    #[test]
    fn a_lone_block_yields_one_part_with_empty_text() {
        let parts = parse_response(r#"<a2ui-json>[{"id": "test"}]</a2ui-json>"#).unwrap();
        assert_eq!(parts.len(), 1);
        assert_eq!(parts[0].text, "");
        assert_eq!(parts[0].a2ui, Some(vec![json!({"id": "test"})]));
    }

    #[test]
    fn leading_and_trailing_text_are_separate_parts() {
        let parts =
            parse_response("Hello\n<a2ui-json>[{\"id\": \"t\"}]</a2ui-json>\nGoodbye").unwrap();
        assert_eq!(parts.len(), 2);
        assert_eq!(parts[0].text, "Hello");
        assert!(parts[0].a2ui.is_some());
        assert_eq!(parts[1].text, "Goodbye");
        assert!(parts[1].a2ui.is_none());
    }

    #[test]
    fn markdown_fences_inside_a_block_are_stripped() {
        let parts =
            parse_response("Text\n<a2ui-json>\n```json\n[{\"id\": \"t\"}]\n```\n</a2ui-json>")
                .unwrap();
        assert_eq!(parts[0].text, "Text");
        assert_eq!(parts[0].a2ui, Some(vec![json!({"id": "t"})]));
    }

    #[test]
    fn a_close_tag_inside_a_string_does_not_end_the_block() {
        let content = concat!(
            r#"<a2ui-json>[{"id": "t", "component": "Text", "#,
            r#""text": "a literal </a2ui-json> inside"}]</a2ui-json>"#
        );
        let parts = parse_response(content).unwrap();
        assert_eq!(parts.len(), 1);
        let messages = parts[0].a2ui.as_ref().unwrap();
        assert_eq!(messages[0]["text"], "a literal </a2ui-json> inside");
    }

    #[test]
    fn missing_empty_and_unterminated_blocks_all_error() {
        assert!(parse_response("").is_err());
        assert!(parse_response("Only text.").is_err());
        assert!(parse_response("<a2ui-json></a2ui-json>").is_err());
        assert!(parse_response(r#"<a2ui-json>[{"id": "t"}]"#).is_err());
        assert!(parse_response("<a2ui-json>\ninvalid_json\n</a2ui-json>").is_err());
    }

    #[test]
    fn has_parts_needs_both_tags() {
        assert!(has_a2ui_parts("Hello <a2ui-json>[]</a2ui-json> World"));
        assert!(!has_a2ui_parts("Hello World"));
        assert!(!has_a2ui_parts("Hello <a2ui-json> World"));
    }

    #[test]
    fn repairs_are_applied_only_when_needed() {
        assert_eq!(
            parse_and_fix(r#"[{"type": "Text", "text": "Hello"},]"#).unwrap(),
            vec![json!({"type": "Text", "text": "Hello"})]
        );
        assert_eq!(
            parse_and_fix(r#"{"type": "Text"}"#).unwrap(),
            vec![json!({"type": "Text"})]
        );
        assert_eq!(
            parse_and_fix("{\"type\": \u{201C}Text\u{201D}, \"other\": \"Value\u{2019}s\"}")
                .unwrap(),
            vec![json!({"type": "Text", "other": "Value's"})]
        );
    }

    #[test]
    fn commas_inside_strings_survive_the_repair() {
        assert_eq!(
            parse_and_fix(r#"{"text": "Hello, world", "array": ["a,b", "c"]}"#).unwrap(),
            vec![json!({"text": "Hello, world", "array": ["a,b", "c"]})]
        );
        // Even when the repair pass has to run because of a real trailing comma.
        assert_eq!(
            parse_and_fix(r#"{"text": "a, b", "list": [1, 2,],}"#).unwrap(),
            vec![json!({"text": "a, b", "list": [1, 2]})]
        );
    }

    #[test]
    fn nested_trailing_commas_are_removed() {
        assert_eq!(
            parse_and_fix(r#"{"a": {"b": 1,}}"#).unwrap(),
            vec![json!({"a": {"b": 1}})]
        );
        assert_eq!(
            parse_and_fix(r#"[{"a": [1, 2, 3,]}]"#).unwrap(),
            vec![json!({"a": [1, 2, 3]})]
        );
    }

    #[test]
    fn open_tags_with_attributes_match_but_lookalikes_do_not() {
        let parts =
            parse_response(r#"<a2ui-json version="v0.9">[{"id":"t"}]</a2ui-json>"#).unwrap();
        assert_eq!(parts.len(), 1);
        assert!(parse_response(r#"<a2ui-jsonx>[]</a2ui-jsonx>"#).is_err());
    }

    /// Two scanners, one grammar.
    ///
    /// [`match_open_tag`] matches a tag anchored at a position in a `&[char]`;
    /// [`crate::toolkit::streaming`]'s searches for one by byte offset and
    /// additionally has to recognise a tag that a chunk boundary cut in half.
    /// Neither can be written in terms of the other, so what keeps them honest
    /// is this: a tolerance added to one and forgotten in the other would mean
    /// a model's output rendering on one path and vanishing on the other.
    #[test]
    fn the_streaming_scanner_opens_a_block_on_exactly_the_tags_this_one_does() {
        const BODY: &str =
            r#"[{"version":"v0.9","createSurface":{"surfaceId":"s1","catalogId":"c"}}]"#;

        for tag in [
            "<a2ui-json>",
            "<A2UI-JSON>",
            r#"<a2ui-json version="v0.9">"#,
            "<a2ui-json  >",
            "<a2ui-jsonx>",
            "<a2ui-json_x>",
            "<a2ui-jso>",
            "<not-a2ui-json>",
        ] {
            let content = format!("hello {tag}{BODY}</a2ui-json>");

            let whole = unwrap_response(&content).is_ok();
            let streamed =
                crate::toolkit::streaming::StreamParser::new(crate::catalog::Catalog::basic())
                    .process_chunk(&content)
                    .expect("a tag this scanner rejects is conversational text, not an error")
                    .iter()
                    .any(|part| part.a2ui.is_some());

            assert_eq!(whole, streamed, "the two scanners disagree about {tag:?}");
        }
    }

    #[test]
    fn multiple_blocks_keep_their_leading_text() {
        let content = "Part 1\n<a2ui-json>\n[{\"id\": \"1\"}]\n</a2ui-json>\nPart 2\n\
                       <a2ui-json>\n[{\"id\": \"2\"}]\n</a2ui-json>\nPart 3";
        let parts = parse_response(content).unwrap();
        let texts: Vec<&str> = parts.iter().map(|p| p.text.as_str()).collect();
        assert_eq!(texts, vec!["Part 1", "Part 2", "Part 3"]);
        assert_eq!(parts[1].a2ui, Some(vec![json!({"id": "2"})]));
        assert_eq!(parts[2].a2ui, None);
    }
}
