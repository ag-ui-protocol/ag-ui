[package]
name = "ag-ui-xtask"
description = "Repo automation: protocol drift detection against the upstream TypeScript source of truth."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
serde.workspace = true
serde_json.workspace = true
# Only `--upstream` and `--refresh` reach the network; the CI gate is offline.
ureq = "3.4.0"
