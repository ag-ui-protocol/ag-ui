package tools_test

// Note: The internal version parsing and constraint matching functions are not exported,
// so they cannot be tested from external test packages. This is intentional as they are
// implementation details that don't need to be publicly tested.
